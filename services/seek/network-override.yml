services:
  db:
    networks:
      - digitaltwins-platform
  seek:
    networks:
      - digitaltwins-platform
  workers:
    networks:
      - digitaltwins-platform
  solr:
    networks:
      - digitaltwins-platform

networks:
  digitaltwins-platform:
    driver: bridge
    name: ${PROJECT_NAME}
