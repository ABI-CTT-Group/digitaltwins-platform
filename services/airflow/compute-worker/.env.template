# Walled-garden IP of the platform VM (drai_mp or drai_portal)
MAIN_VM_IP=10.2.0.170

# Airflow UID on the host
AIRFLOW_UID=1001

# Ports — must match the platform .env
AIRFLOW_PORT=8002
AIRFLOW_POSTGRES_PORT=8013
REDIS_PORT=8016
MINIO_PORT=8011
DIGITALTWINS_API_PORT=8010

# Secrets — must match the platform .env exactly
AIRFLOW_POSTGRES_PASSWORD=<same_as_platform>
REDIS_PASSWORD=<same_as_platform>
AIRFLOW__CORE__FERNET_KEY=<same_as_platform>
AIRFLOW__API_AUTH__JWT_SECRET=<same_as_platform>
AIRFLOW__API__SECRET_KEY=<same_as_platform>
MINIO_SERVER_ACCESS_KEY=<same_as_platform>
MINIO_SERVER_SECRET_KEY=<same_as_platform>
