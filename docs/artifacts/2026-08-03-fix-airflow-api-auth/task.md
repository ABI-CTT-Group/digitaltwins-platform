# Tasks

- [x] Modify `preprocessor.py` — replace `_get_api_token` and env vars to use client_credentials grant
- [x] Modify Airflow `docker-compose.yml` — pass Keycloak env vars to containers
- [x] Verify no lint/syntax errors
